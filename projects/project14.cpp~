#include <iostream>

using namespace std;
//q1//
int factorial(int num) {

	int fact=1;

	for (int i = num; i >= 1; i--)
		//starts at number then counts down/
		fact = fact*i;
		//multiplies the two nums// 
		return fact;
			}
//q2//
void sign(int a){
	
	if ( a>0 ){

		cout << "No parking\n";
		sign(--a);
		}
	}

int main()
{
	int num;
	cout << "Enter a number and it will display its factorial" << endl;
	//enter a num//
	cin >> num;
	//runs function through num//
	cout << endl << factorial(num) << endl;



	int number;

	cout << " Enter a number \n";
	cin >> number;
	sign(number);
	return 0;

}

